# Copy this file to .env and fill in your key. The .env is gitignored — NEVER commit it.
# The variable name must match api_key_env in config.toml (default: DEEPSEEK_API_KEY).
DEEPSEEK_API_KEY=sk-...
# GROQ_API_KEY=gsk_...   # only needed if you set provider = "groq"
